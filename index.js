const { Handler } = require("./src/Handler");
const { HydraFriend, initialize } = require("./src/HydraFriend");
const { Shape } = require("./src/Shape");

module.exports = {
    Handler,
    HydraFriend,
    initialize,
    Shape,
};